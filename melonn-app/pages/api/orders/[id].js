import db from "../../../../utils/fb-admin-key";

export default async (req, res) => {
    if (req.method === "GET") {
        try {
            

        } catch (error) {
            return res.json({ error });

        }
    }
};